###############################################################################
## 应用名称
spring:
  application:
    name: p2p-middleground-sms

###############################################################################
## cloud 配置文件
#  zipkin:
#    base-url: http://127.0.0.1:8766/
#  sleuth:
#    sampler:
#      percentage: 1 #zipkin采样率，默认为0.1，开发阶段改为1后全采样，但是会降低接口调用效率

  cloud:
    config:
      name: ${spring.application.name}
      profile:  dev
      label:  master
      uri:  http://10.13.4.56:8762/
#      discovery:
#        enabled:  true
#        serviceId:  lljr-config-server
  rabbitmq:
    host: 10.13.4.55
    port: 5672
    username: admin
    password: admin
#  zipkin:
#    rabbitmq:
#      queue: zipkin

###############################################################################
server:
#  port: ${port:0}  # ${port:0}
  port: 8002

###############################################################################
## eureka 配置中心
eureka:
  instance:
#    hostname: ${client.ip:127.0.0.1}
#    instanceId: ${spring.application.name}:${client.ip:127.0.0.1}:${server.port}
    prefer-ip-address: true # 访问路径可以显示IP地址
  client:
    serviceUrl:
      defaultZone: http://10.13.4.56:8761/eureka

feign:
  hystrix:
    enabled:  true